/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: [
          "cdn.pandascore.co",
          "dummyimage.com"
        ],
      },
}

module.exports = nextConfig
